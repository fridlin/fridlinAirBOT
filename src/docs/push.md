git status
node scripts/ship.js patch


tree -a -I 'node_modules|.git'


1️⃣ Проверка перед релизом
git status


Должны быть изменения, но без коммитов.

2️⃣ Единственная команда для пуша
node scripts/ship.js minor


(если багфикс — patch, если ломающие изменения — major)

3️⃣ Когда ship спросит commit message — вводишь:
feat: normalize wind to km/h and align feels-like, trends, and ship workflow


4️⃣ Ship сделает всё сам

обновит VERSION

обновит CHANGELOG

закоммитит все изменения

запушит в main

👉 Ручных команд больше нет.



Если код оказался в main, значит он прошёл через ship.

Никаких:

git push

git commit

git add

вручную.